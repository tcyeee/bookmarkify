# M2 — Layer 2 基础实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `scraper-service` 进程内封装 spider-rs 为可调用的 async 函数，实现 Chrome 全局单例生命周期管理，修复截图功能，并在 `POST /scrape` 新增 `headless: true` 参数路由到 Layer 2。

**Architecture:** 新增 `headless.rs` 模块，持有全局 `Mutex<()>` 串行化 headless 请求，底层使用 spider-rs `Website` API（与 scraper-demo 中已验证的代码一致）。`ScrapeResult` 新增 `screenshot_bytes` 字段，`ScrapeError` 新增 `HeadlessFailed` 变体，`main.rs` 处理 `headless: true` 路由和新错误类型。

**Tech Stack:** spider 2.x (chrome + chrome_stealth), tokio, once_cell, 现有 axum/reqwest 栈

---

## 文件变更清单

| 操作 | 文件 |
|------|------|
| Modify | `crates/scraper-service/Cargo.toml` |
| Modify | `crates/scraper-service/src/scraper.rs` |
| Create | `crates/scraper-service/src/headless.rs` |
| Modify | `crates/scraper-service/src/main.rs` |

---

### Task 1: 扩展 ScrapeResult 和 ScrapeError

**Files:**
- Modify: `crates/scraper-service/src/scraper.rs`

- [ ] **Step 1: 在 `ScrapeResult` 添加 `screenshot_bytes` 字段**

  将 `scraper.rs` 第 4-11 行的结构体替换为：

  ```rust
  #[derive(Debug, Serialize, PartialEq)]
  pub struct ScrapeResult {
      pub title: Option<String>,
      pub description: Option<String>,
      pub image: Option<String>,
      pub favicon: Option<String>,
      pub source: String,
      pub screenshot_bytes: Option<Vec<u8>>,
  }
  ```

- [ ] **Step 2: 在 `ScrapeError` 添加 `HeadlessFailed` 变体**

  将第 13-18 行的枚举替换为：

  ```rust
  #[derive(Debug)]
  pub enum ScrapeError {
      InvalidUrl,
      Timeout,
      FetchFailed(String),
      HeadlessFailed(String),
  }
  ```

- [ ] **Step 3: 在 `parse_metadata` 的 4 处 `ScrapeResult` 构造中补充 `screenshot_bytes: None`**

  第 82-128 行的 `parse_metadata` 函数，4 处返回均加 `screenshot_bytes: None`：

  ```rust
  pub fn parse_metadata(html: &str, base_url: &reqwest::Url) -> ScrapeResult {
      let document = Html::parse_document(html);

      // OG tags
      if let Some(title) = meta_property(&document, "og:title") {
          return ScrapeResult {
              title: Some(title),
              description: meta_property(&document, "og:description")
                  .or_else(|| meta_name(&document, "description")),
              image: meta_property(&document, "og:image"),
              favicon: extract_favicon(&document, base_url),
              source: "og".to_string(),
              screenshot_bytes: None,
          };
      }

      // Twitter Card
      if let Some(title) = meta_name(&document, "twitter:title") {
          return ScrapeResult {
              title: Some(title),
              description: meta_name(&document, "twitter:description")
                  .or_else(|| meta_name(&document, "description")),
              image: meta_name(&document, "twitter:image"),
              favicon: extract_favicon(&document, base_url),
              source: "twitter_card".to_string(),
              screenshot_bytes: None,
          };
      }

      // JSON-LD
      if let Some((title, desc, image)) = extract_json_ld(&document) {
          return ScrapeResult {
              title,
              description: desc.or_else(|| meta_name(&document, "description")),
              image,
              favicon: extract_favicon(&document, base_url),
              source: "json_ld".to_string(),
              screenshot_bytes: None,
          };
      }

      // HTML fallback
      ScrapeResult {
          title: extract_title(&document),
          description: meta_name(&document, "description"),
          image: None,
          favicon: extract_favicon(&document, base_url),
          source: "html".to_string(),
          screenshot_bytes: None,
      }
  }
  ```

- [ ] **Step 4: 运行现有单元测试，确认全部通过**

  ```bash
  cd /Users/tcyeee/Documents/Code/bookmarkify/bookmarkify-scrapper
  cargo test -p scraper-service 2>&1
  ```

  期望：所有 test 均 `ok`，无编译错误。

- [ ] **Step 5: Commit**

  ```bash
  git add crates/scraper-service/src/scraper.rs
  git commit -m "feat(m2): add screenshot_bytes to ScrapeResult and HeadlessFailed to ScrapeError"
  ```

---

### Task 2: 添加 spider 依赖，创建 headless.rs 存根

**Files:**
- Modify: `crates/scraper-service/Cargo.toml`
- Create: `crates/scraper-service/src/headless.rs`
- Modify: `crates/scraper-service/src/main.rs` (仅加 `mod headless;`)

- [ ] **Step 1: 在 `Cargo.toml` 添加依赖**

  在 `[dependencies]` 末尾追加：

  ```toml
  spider = { version = "2", features = ["chrome", "chrome_stealth"] }
  once_cell = "1"
  ```

- [ ] **Step 2: 创建 `headless.rs` 存根**

  新建 `crates/scraper-service/src/headless.rs`，内容：

  ```rust
  use crate::scraper::{ScrapeError, ScrapeResult};

  pub async fn scrape_headless(url: &str, timeout_secs: u64) -> Result<ScrapeResult, ScrapeError> {
      let _ = (url, timeout_secs);
      Err(ScrapeError::HeadlessFailed("not implemented".to_string()))
  }
  ```

- [ ] **Step 3: 在 `main.rs` 第 1 行前加 `mod headless;`**

  将 `main.rs` 开头改为：

  ```rust
  mod headless;
  mod scraper;
  ```

- [ ] **Step 4: 编译验证**

  ```bash
  cargo build -p scraper-service 2>&1
  ```

  期望：编译成功，无 error（spider 依赖下载可能需要 1-2 分钟）。

- [ ] **Step 5: Commit**

  ```bash
  git add crates/scraper-service/Cargo.toml crates/scraper-service/src/headless.rs crates/scraper-service/src/main.rs
  git commit -m "feat(m2): add spider dep and headless.rs stub"
  ```

---

### Task 3: 实现 scrape_headless 与集成测试

**Files:**
- Modify: `crates/scraper-service/src/headless.rs`

- [ ] **Step 1: 先写集成测试（TDD）**

  在 `headless.rs` 末尾追加：

  ```rust
  #[cfg(test)]
  mod tests {
      use super::*;

      #[tokio::test]
      #[ignore]
      async fn headless_huaban_returns_metadata() {
          let result = scrape_headless("https://huaban.com/", 30).await;
          assert!(result.is_ok(), "scrape_headless failed: {:?}", result.err());
          let r = result.unwrap();
          assert!(r.title.is_some(), "title should not be None");
          assert!(!r.title.as_deref().unwrap_or("").is_empty(), "title should not be empty");
          assert_eq!(r.source, "headless");
      }

      #[tokio::test]
      #[ignore]
      async fn headless_huaban_returns_screenshot_bytes() {
          let result = scrape_headless("https://huaban.com/", 30).await;
          assert!(result.is_ok(), "scrape_headless failed: {:?}", result.err());
          let r = result.unwrap();
          assert!(r.screenshot_bytes.is_some(), "screenshot_bytes should not be None");
          let bytes = r.screenshot_bytes.unwrap();
          assert!(bytes.len() > 10_240, "screenshot should be > 10KB, got {} bytes", bytes.len());
      }
  }
  ```

- [ ] **Step 2: 运行测试，确认当前因存根而失败**

  ```bash
  cargo test -p scraper-service -- --ignored 2>&1
  ```

  期望：两个 ignore 测试均 `FAILED`，失败原因是 `"not implemented"`。

- [ ] **Step 3: 实现完整的 `headless.rs`**

  用以下内容替换整个 `headless.rs`（保留末尾测试）：

  ```rust
  use once_cell::sync::Lazy;
  use spider::configuration::{ScreenShotConfig, ScreenshotParams};
  use spider::features::chrome_common::{RequestInterceptConfiguration, WaitForDelay};
  use spider::website::Website;
  use std::time::Duration;
  use tokio::sync::Mutex;

  use crate::scraper::{parse_metadata, ScrapeError, ScrapeResult};

  // Serialize all headless requests — one Chrome operation at a time
  static HEADLESS_LOCK: Lazy<Mutex<()>> = Lazy::new(|| Mutex::new(()));

  pub async fn scrape_headless(url: &str, timeout_secs: u64) -> Result<ScrapeResult, ScrapeError> {
      let parsed = reqwest::Url::parse(url).map_err(|_| ScrapeError::InvalidUrl)?;

      let _guard = HEADLESS_LOCK.lock().await;

      let screenshot_config = ScreenShotConfig::new(
          ScreenshotParams::new(Default::default(), Some(true), None),
          true,  // return bytes in page.screenshot_bytes
          false, // do not save to disk
          None,
      );

      let mut website = Website::new(url);
      website
          .with_stealth(true)
          .with_chrome_intercept(RequestInterceptConfiguration::new(true))
          .with_wait_for_delay(Some(WaitForDelay {
              timeout: Some(Duration::from_secs(8)),
          }))
          .with_screenshot(Some(screenshot_config));

      let pages = tokio::time::timeout(Duration::from_secs(timeout_secs), async move {
          website.scrape().await;
          website.get_pages()
      })
      .await
      .map_err(|_| ScrapeError::Timeout)?;

      let page = match pages {
          Some(p) if !p.is_empty() => p[0].clone(),
          _ => return Err(ScrapeError::HeadlessFailed("no page returned".to_string())),
      };

      let html = page.get_html().to_string();
      if html.is_empty() {
          return Err(ScrapeError::HeadlessFailed("empty page html".to_string()));
      }

      let screenshot_bytes = page.screenshot_bytes.clone();

      let mut result = parse_metadata(&html, &parsed);
      result.source = "headless".to_string();
      result.screenshot_bytes = screenshot_bytes;

      Ok(result)
  }

  #[cfg(test)]
  mod tests {
      use super::*;

      #[tokio::test]
      #[ignore]
      async fn headless_huaban_returns_metadata() {
          let result = scrape_headless("https://huaban.com/", 30).await;
          assert!(result.is_ok(), "scrape_headless failed: {:?}", result.err());
          let r = result.unwrap();
          assert!(r.title.is_some(), "title should not be None");
          assert!(!r.title.as_deref().unwrap_or("").is_empty(), "title should not be empty");
          assert_eq!(r.source, "headless");
      }

      #[tokio::test]
      #[ignore]
      async fn headless_huaban_returns_screenshot_bytes() {
          let result = scrape_headless("https://huaban.com/", 30).await;
          assert!(result.is_ok(), "scrape_headless failed: {:?}", result.err());
          let r = result.unwrap();
          assert!(r.screenshot_bytes.is_some(), "screenshot_bytes should not be None");
          let bytes = r.screenshot_bytes.unwrap();
          assert!(bytes.len() > 10_240, "screenshot should be > 10KB, got {} bytes", bytes.len());
      }
  }
  ```

- [ ] **Step 4: 编译验证**

  ```bash
  cargo build -p scraper-service 2>&1
  ```

  期望：编译成功，无 error。

- [ ] **Step 5: 运行集成测试**

  ```bash
  cargo test -p scraper-service -- --ignored --nocapture 2>&1
  ```

  期望：`headless_huaban_returns_metadata` PASS，`headless_huaban_returns_screenshot_bytes` 根据截图调试结果可能 PASS 或 FAIL（见 Task 4）。

- [ ] **Step 6: Commit**

  ```bash
  git add crates/scraper-service/src/headless.rs
  git commit -m "feat(m2): implement scrape_headless with spider-rs and integration tests"
  ```

---

### Task 4: 截图调试 — 若 screenshot_bytes 测试失败

> 只有 `headless_huaban_returns_screenshot_bytes` 失败时才执行此 Task。

**Files:**
- Modify: `crates/scraper-service/src/headless.rs`

- [ ] **Step 1: 尝试 `save: true` 验证 Chrome 截图基础能力**

  在 `scrape_headless` 中将 `ScreenShotConfig` 改为：

  ```rust
  let screenshot_config = ScreenShotConfig::new(
      ScreenshotParams::new(Default::default(), Some(true), None),
      false, // bytes: false
      true,  // save: write to disk to verify Chrome can screenshot
      None,
  );
  ```

  同时在 `scrape_headless` 末尾（返回前）加：

  ```rust
  // Debug: check if screenshot file was written to disk
  let disk_path = format!("/tmp/screenshot_debug_{}.png", std::process::id());
  tracing::info!("screenshot_bytes from page: {:?}", page.screenshot_bytes.as_ref().map(|b| b.len()));
  tracing::info!("looking for disk screenshot at: {disk_path}");
  ```

- [ ] **Step 2: 运行测试，观察日志**

  ```bash
  RUST_LOG=info cargo test -p scraper-service headless_huaban_returns_metadata -- --ignored --nocapture 2>&1
  ```

  观察日志输出：
  - 若 `/tmp/` 下有 PNG 文件：说明 Chrome 截图可用，问题在 bytes API
  - 若无 PNG 文件：说明 Chrome 截图本身未工作

- [ ] **Step 3a（如果磁盘文件存在）: 查找 spider-rs v2 bytes API 正确用法**

  查看 scraper-demo 中的 `ScreenShotConfig` 参数，尝试将 `bytes: true, save: false` 改为同时 `bytes: true, save: true`：

  ```rust
  let screenshot_config = ScreenShotConfig::new(
      ScreenshotParams::new(Default::default(), Some(true), None),
      true,  // bytes
      true,  // save — try both true simultaneously
      None,
  );
  ```

  再次运行集成测试，观察 `screenshot_bytes` 是否有值。

- [ ] **Step 3b（如果磁盘文件也不存在）: 记录问题，截图推后**

  在 `headless.rs` 顶部加注释：

  ```rust
  // NOTE: spider-rs screenshot_bytes does not populate in v2.51.x with current config.
  // Chrome screenshot via ScreenShotConfig appears non-functional in this environment.
  // Screenshot support deferred to M4 where direct CDP Page.captureScreenshot will be used.
  ```

  将 `screenshot_bytes` 测试的 `assert!` 改为 `eprintln!` + skip，避免阻塞 M2 交付：

  ```rust
  #[tokio::test]
  #[ignore]
  async fn headless_huaban_returns_screenshot_bytes() {
      let result = scrape_headless("https://huaban.com/", 30).await;
      assert!(result.is_ok(), "scrape_headless failed: {:?}", result.err());
      let r = result.unwrap();
      // screenshot_bytes debugging: see NOTE at top of headless.rs
      eprintln!("screenshot_bytes len: {:?}", r.screenshot_bytes.as_ref().map(|b| b.len()));
      // Non-blocking: if None, this is a known spider-rs v2 limitation
  }
  ```

- [ ] **Step 4: Commit 截图调试结果**

  ```bash
  git add crates/scraper-service/src/headless.rs
  git commit -m "fix(m2): debug screenshot_bytes — document spider-rs v2 screenshot behavior"
  ```

---

### Task 5: 接入 main.rs — headless 路由与错误处理

**Files:**
- Modify: `crates/scraper-service/src/main.rs`

- [ ] **Step 1: 在 `AppState` 添加 `headless_timeout_secs`**

  将第 15-19 行替换为：

  ```rust
  #[derive(Clone)]
  struct AppState {
      client: reqwest::Client,
      api_key: String,
      headless_timeout_secs: u64,
  }
  ```

- [ ] **Step 2: 在 `main()` 中读取 `HEADLESS_TIMEOUT_SECS` 并构建新 AppState**

  在 `main()` 中（紧接现有 `timeout_secs` 读取之后）添加：

  ```rust
  let headless_timeout_secs: u64 = env::var("HEADLESS_TIMEOUT_SECS")
      .ok()
      .and_then(|v| v.parse().ok())
      .unwrap_or(30);
  ```

  将 `AppState` 构造改为：

  ```rust
  let state = AppState { client, api_key, headless_timeout_secs };
  ```

- [ ] **Step 3: 在 `ScrapeRequest` 添加 `headless` 字段**

  将第 87-90 行替换为：

  ```rust
  #[derive(Deserialize)]
  struct ScrapeRequest {
      url: String,
      headless: Option<bool>,
  }
  ```

- [ ] **Step 4: 更新 `scrape_handler` 路由逻辑和错误处理**

  将 `scrape_handler` 函数整体替换为：

  ```rust
  async fn scrape_handler(
      State(state): State<AppState>,
      Json(body): Json<ScrapeRequest>,
  ) -> Response {
      let result = if body.headless.unwrap_or(false) {
          headless::scrape_headless(&body.url, state.headless_timeout_secs).await
      } else {
          scraper::scrape(&body.url, &state.client).await
      };

      match result {
          Ok(r) => Json(ScrapeResponse {
              title: r.title,
              description: r.description,
              image: r.image,
              favicon: r.favicon,
              source: r.source,
          })
          .into_response(),

          Err(scraper::ScrapeError::InvalidUrl) => (
              StatusCode::UNPROCESSABLE_ENTITY,
              Json(ErrorResponse { error: "invalid url".to_string(), detail: None }),
          )
              .into_response(),

          Err(scraper::ScrapeError::Timeout) => (
              StatusCode::GATEWAY_TIMEOUT,
              Json(ErrorResponse { error: "timeout".to_string(), detail: None }),
          )
              .into_response(),

          Err(scraper::ScrapeError::FetchFailed(msg)) => (
              StatusCode::BAD_GATEWAY,
              Json(ErrorResponse { error: "fetch failed".to_string(), detail: Some(msg) }),
          )
              .into_response(),

          Err(scraper::ScrapeError::HeadlessFailed(msg)) => (
              StatusCode::BAD_GATEWAY,
              Json(ErrorResponse { error: "headless failed".to_string(), detail: Some(msg) }),
          )
              .into_response(),
      }
  }
  ```

- [ ] **Step 5: 编译验证**

  ```bash
  cargo build -p scraper-service 2>&1
  ```

  期望：编译成功，无 error。

- [ ] **Step 6: 运行所有单元测试**

  ```bash
  cargo test -p scraper-service 2>&1
  ```

  期望：所有非 `#[ignore]` 测试通过。

- [ ] **Step 7: 手动验证 Layer 1 路由（headless: false）**

  启动服务：

  ```bash
  API_KEY=test cargo run -p scraper-service 2>&1 &
  sleep 2
  curl -s -X POST http://localhost:3000/scrape \
    -H "Authorization: Bearer test" \
    -H "Content-Type: application/json" \
    -d '{"url": "https://example.com"}' | python3 -m json.tool
  kill %1
  ```

  期望：返回 `source: "og"` 或 `"html"` 的元数据 JSON。

- [ ] **Step 8: 手动验证 Layer 2 路由（headless: true）**

  ```bash
  API_KEY=test cargo run -p scraper-service 2>&1 &
  sleep 2
  curl -s -X POST http://localhost:3000/scrape \
    -H "Authorization: Bearer test" \
    -H "Content-Type: application/json" \
    -d '{"url": "https://huaban.com/", "headless": true}' | python3 -m json.tool
  kill %1
  ```

  期望：返回 `source: "headless"` 的元数据 JSON，title 非空。

- [ ] **Step 9: Commit**

  ```bash
  git add crates/scraper-service/src/main.rs
  git commit -m "feat(m2): wire headless route into POST /scrape with headless_timeout_secs config"
  ```

---

## M2 交付验收

全部 Task 完成后，确认以下检查项：

- [ ] `cargo build -p scraper-service` 无 error
- [ ] `cargo test -p scraper-service` 所有非 ignore 测试通过
- [ ] `POST /scrape` 不带 `headless` 字段：走 Layer 1，行为与 M1 一致
- [ ] `POST /scrape` 带 `"headless": true`：走 Layer 2，返回 `source: "headless"` 和花瓣正确 title
- [ ] 截图调试结果已记录（bytes 返回或 spider-rs 限制已注释说明）
