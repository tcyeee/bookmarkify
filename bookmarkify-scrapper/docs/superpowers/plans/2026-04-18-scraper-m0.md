# Scraper M0 Demo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 spider-rs + Chrome stealth 验证能否抓取花瓣、知乎页面元数据并截图保存到本地。

**Architecture:** 单 Cargo workspace，一个 `scraper-demo` binary crate。main.rs 顺序抓取两个目标 URL，提取元数据，截图保存到 `screenshots/`，结果以 JSON 打印到 stdout。

**Tech Stack:** Rust 1.90, spider v2 (features: chrome, chrome_stealth), tokio, serde_json, Google Chrome（`/Applications/Google Chrome.app`）

---

## File Map

| 路径 | 操作 | 职责 |
|------|------|------|
| `Cargo.toml` | 创建 | workspace root |
| `.gitignore` | 创建 | 忽略 target/ 和 screenshots/ |
| `crates/scraper-demo/Cargo.toml` | 创建 | demo crate 依赖声明 |
| `crates/scraper-demo/src/main.rs` | 创建 | 抓取 + 截图 + 打印结果 |
| `screenshots/` | 创建（目录） | 截图输出，不纳入 git |

---

### Task 1: 初始化 Cargo Workspace

**Files:**
- Create: `Cargo.toml`
- Create: `.gitignore`
- Create: `crates/scraper-demo/Cargo.toml`
- Create: `crates/scraper-demo/src/main.rs`（占位）

- [ ] **Step 1: 创建 workspace Cargo.toml**

```toml
# /Cargo.toml
[workspace]
members = ["crates/scraper-demo"]
resolver = "2"
```

- [ ] **Step 2: 创建 .gitignore**

```gitignore
/target
screenshots/
```

- [ ] **Step 3: 创建 crate 目录和 Cargo.toml**

```bash
mkdir -p crates/scraper-demo/src
```

```toml
# crates/scraper-demo/Cargo.toml
[package]
name = "scraper-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
spider = { version = "2", features = ["chrome", "chrome_stealth"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
```

- [ ] **Step 4: 写最小占位 main.rs 让 workspace 编译通过**

```rust
// crates/scraper-demo/src/main.rs
fn main() {
    println!("scraper-demo placeholder");
}
```

- [ ] **Step 5: 验证 workspace 编译**

```bash
cargo build
```

期望输出：`Compiling scraper-demo v0.1.0` 后出现 `Finished`，无 error。

> 注意：spider v2 依赖较多，首次编译耗时 2–5 分钟。

- [ ] **Step 6: 创建截图输出目录**

```bash
mkdir -p screenshots
```

- [ ] **Step 7: Commit**

```bash
git add Cargo.toml Cargo.lock .gitignore crates/
git commit -m "chore: init cargo workspace with scraper-demo crate"
```

---

### Task 2: 实现单 URL 抓取 + 元数据提取

**Files:**
- Modify: `crates/scraper-demo/src/main.rs`

- [ ] **Step 1: 替换 main.rs，实现单 URL 抓取**

```rust
// crates/scraper-demo/src/main.rs
use spider::website::Website;
use std::time::Duration;

#[tokio::main]
async fn main() {
    let url = "https://www.zhihu.com/";

    println!("抓取: {url}");

    let mut website = Website::new(url);
    website
        .with_chrome_intercept(true, true)   // 启用 stealth
        .with_wait_for_delay(Some(spider::configuration::WaitForDelay {
            timeout: Some(Duration::from_secs(8)),
        }));

    website.scrape().await;

    let pages = website.get_pages();
    match pages {
        Some(pages) if !pages.is_empty() => {
            let html = pages[0].get_html();
            println!("HTML 长度: {} bytes", html.len());
            println!("HTML 前 300 字符:\n{}", &html[..html.len().min(300)]);
        }
        _ => eprintln!("未获取到页面"),
    }
}
```

- [ ] **Step 2: 编译确认无错**

```bash
cargo build
```

期望：`Finished` 无 error。

- [ ] **Step 3: 运行，观察是否能拿到知乎 HTML**

```bash
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" cargo run
```

期望：打印 `HTML 长度:` 且长度 > 1000 bytes，内容包含 `知乎` 或正常页面 HTML，不是反爬拦截页（拦截页通常包含 `robot` / `验证` / `captcha`）。

> 如果 spider 找不到 Chrome，设置环境变量 `CHROME` 指向 Chrome 可执行文件路径。

- [ ] **Step 4: Commit**

```bash
git add crates/scraper-demo/src/main.rs
git commit -m "feat(m0): fetch single URL with spider-rs chrome stealth"
```

---

### Task 3: 元数据提取（title / description / og:image）

**Files:**
- Modify: `crates/scraper-demo/src/main.rs`

- [ ] **Step 1: 添加 HTML 解析，提取元数据**

用正则提取（不引入额外 HTML 解析库，保持依赖最小）：

```rust
// crates/scraper-demo/src/main.rs
use spider::website::Website;
use std::time::Duration;

fn extract_meta(html: &str, property: &str) -> Option<String> {
    // 匹配 <meta property="og:xxx" content="..."> 或 <meta name="xxx" content="...">
    let pattern = format!(
        r#"(?:property|name)=["\']{}["\'][^>]*content=["\']([^"\']+)["\']|content=["\']([^"\']+)["\'][^>]*(?:property|name)=["\']{}["\']"#,
        property, property
    );
    let re = regex::Regex::new(&pattern).ok()?;
    re.captures(html)
        .and_then(|c| c.get(1).or(c.get(2)))
        .map(|m| m.as_str().to_string())
}

fn extract_title(html: &str) -> Option<String> {
    let re = regex::Regex::new(r"<title[^>]*>([^<]+)</title>").ok()?;
    re.captures(html)
        .and_then(|c| c.get(1))
        .map(|m| m.as_str().trim().to_string())
}

#[tokio::main]
async fn main() {
    let url = "https://www.zhihu.com/";

    let mut website = Website::new(url);
    website
        .with_chrome_intercept(true, true)
        .with_wait_for_delay(Some(spider::configuration::WaitForDelay {
            timeout: Some(Duration::from_secs(8)),
        }));

    website.scrape().await;

    let pages = website.get_pages();
    let html = match pages {
        Some(pages) if !pages.is_empty() => pages[0].get_html().to_string(),
        _ => {
            eprintln!("未获取到页面");
            return;
        }
    };

    let result = serde_json::json!({
        "url": url,
        "title": extract_title(&html),
        "description": extract_meta(&html, "description").or_else(|| extract_meta(&html, "og:description")),
        "og_image": extract_meta(&html, "og:image"),
    });

    println!("{}", serde_json::to_string_pretty(&result).unwrap());
}
```

- [ ] **Step 2: 在 `crates/scraper-demo/Cargo.toml` 添加 regex 依赖**

```toml
[dependencies]
spider = { version = "2", features = ["chrome", "chrome_stealth"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
regex = "1"
```

- [ ] **Step 3: 编译**

```bash
cargo build
```

期望：`Finished` 无 error。

- [ ] **Step 4: 运行并验证元数据**

```bash
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" cargo run
```

期望 JSON 输出中 `title` 非空且包含"知乎"，不包含"验证码"/"机器人"等拦截关键词。

- [ ] **Step 5: Commit**

```bash
git add crates/scraper-demo/Cargo.toml crates/scraper-demo/src/main.rs
git commit -m "feat(m0): extract title/description/og:image from rendered HTML"
```

---

### Task 4: 截图功能

**Files:**
- Modify: `crates/scraper-demo/src/main.rs`

- [ ] **Step 1: 添加截图逻辑**

spider-rs 通过 `Page::screenshot` 获取截图字节，再写入文件：

```rust
// crates/scraper-demo/src/main.rs
use spider::website::Website;
use spider::page::Page;
use std::time::Duration;
use std::path::Path;

fn extract_meta(html: &str, property: &str) -> Option<String> {
    let pattern = format!(
        r#"(?:property|name)=["\']{}["\'][^>]*content=["\']([^"\']+)["\']|content=["\']([^"\']+)["\'][^>]*(?:property|name)=["\']{}["\']"#,
        property, property
    );
    let re = regex::Regex::new(&pattern).ok()?;
    re.captures(html)
        .and_then(|c| c.get(1).or(c.get(2)))
        .map(|m| m.as_str().to_string())
}

fn extract_title(html: &str) -> Option<String> {
    let re = regex::Regex::new(r"<title[^>]*>([^<]+)</title>").ok()?;
    re.captures(html)
        .and_then(|c| c.get(1))
        .map(|m| m.as_str().trim().to_string())
}

fn domain_from_url(url: &str) -> String {
    url.trim_start_matches("https://")
        .trim_start_matches("http://")
        .split('/')
        .next()
        .unwrap_or("unknown")
        .replace('.', "_")
}

async fn scrape_url(url: &str) -> serde_json::Value {
    println!("\n>>> 开始抓取: {url}");

    let mut website = Website::new(url);
    website
        .with_chrome_intercept(true, true)
        .with_wait_for_delay(Some(spider::configuration::WaitForDelay {
            timeout: Some(Duration::from_secs(8)),
        }));

    website.scrape().await;

    let pages = website.get_pages();
    let page = match pages {
        Some(pages) if !pages.is_empty() => pages[0].clone(),
        _ => {
            eprintln!("未获取到页面: {url}");
            return serde_json::json!({ "url": url, "error": "no page" });
        }
    };

    let html = page.get_html();

    // 截图
    let domain = domain_from_url(url);
    let screenshot_path = format!("screenshots/{domain}.png");
    std::fs::create_dir_all("screenshots").ok();

    let screenshot_result = match page.screenshot(true, None).await {
        Ok(bytes) if !bytes.is_empty() => {
            std::fs::write(&screenshot_path, &bytes).ok();
            Some(screenshot_path.clone())
        }
        _ => None,
    };

    let title = extract_title(html);
    let anti_crawler_bypassed = title
        .as_deref()
        .map(|t| {
            let t_lower = t.to_lowercase();
            !t_lower.contains("robot") && !t_lower.contains("验证") && !t_lower.contains("captcha")
        })
        .unwrap_or(false);

    serde_json::json!({
        "url": url,
        "title": title,
        "description": extract_meta(html, "description").or_else(|| extract_meta(html, "og:description")),
        "og_image": extract_meta(html, "og:image"),
        "screenshot": screenshot_result,
        "anti_crawler_bypassed": anti_crawler_bypassed,
    })
}

#[tokio::main]
async fn main() {
    let targets = [
        "https://huaban.com/",
        "https://www.zhihu.com/",
    ];

    let mut results = Vec::new();
    for url in targets {
        let result = scrape_url(url).await;
        results.push(result);
    }

    println!("\n\n===== M0 验证结果 =====");
    println!("{}", serde_json::to_string_pretty(&results).unwrap());
}
```

- [ ] **Step 2: 编译**

```bash
cargo build
```

期望：`Finished` 无 error。

- [ ] **Step 3: 运行完整验证**

```bash
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" cargo run
```

期望：
- 打印两个 URL 的 JSON 结果
- `screenshots/huaban_com.png` 和 `screenshots/www_zhihu_com.png` 存在
- `ls -lh screenshots/` 显示两个文件均 > 10KB
- 两个 `title` 字段非空
- `anti_crawler_bypassed` 为 `true`

- [ ] **Step 4: 检查截图文件大小**

```bash
ls -lh screenshots/
```

期望：两个 `.png` 文件，每个 > 10KB。

- [ ] **Step 5: Commit**

```bash
git add crates/scraper-demo/src/main.rs
git commit -m "feat(m0): add screenshot capture to local screenshots/ directory"
```

---

### Task 5: M0 验收与结论记录

**Files:**
- Create: `docs/m0-results.md`

- [ ] **Step 1: 运行最终验证，记录真实输出**

```bash
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" cargo run 2>&1 | tee /tmp/m0-output.txt
```

- [ ] **Step 2: 对照成功标准逐项检查**

| 检查项 | 期望 | 实际 |
|--------|------|------|
| 花瓣 title | 非空、非拦截页 | 填入实际值 |
| 知乎 title | 非空、非拦截页 | 填入实际值 |
| 花瓣截图文件 | > 10KB | 填入实际大小 |
| 知乎截图文件 | > 10KB | 填入实际大小 |
| og:image | 至少一个非空 | 填入实际值 |

- [ ] **Step 3: 创建 m0-results.md 记录结论**

将实际运行输出和检查结果填入 `docs/m0-results.md`，格式：

```markdown
# M0 验证结果

**日期:** YYYY-MM-DD
**结论:** PASS / FAIL

## 输出

\`\`\`json
（粘贴实际 JSON 输出）
\`\`\`

## 截图文件大小

（粘贴 ls -lh screenshots/ 输出）

## 结论

spider-rs 在花瓣/知乎上 [能/不能] 绕过反爬。
下一步: M1（继续）/ 评估切换方案（如 headless_chrome）
```

- [ ] **Step 4: Commit 最终**

```bash
git add docs/m0-results.md
git commit -m "docs(m0): record m0 validation results"
```
