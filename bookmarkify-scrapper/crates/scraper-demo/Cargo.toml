[package]
name = "scraper-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
spider = { version = "2", features = ["chrome", "chrome_stealth"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
regex = "1"
once_cell = "1"
