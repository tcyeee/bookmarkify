-- 2026-08-16 — site_display_pref.icon_padding 的列默认值 25 → 0
--
-- 应用时机：**任意**。这一条与部署顺序无关，两个方向都安全 —— 新旧代码写入这一列时都是
-- 显式带值的（`SiteDisplayPrefService.save` 从后台表单原样落库），列默认值实际上没有任何
-- 写入路径会用到。改它纯粹是为了让建表语句与代码里的默认值别再各说各话：
-- 真正在生效的那个默认值在 `SiteDisplayPrefEntity.DEFAULT_ICON_PADDING`（本次一并改为 0），
-- 而库里留着 25 会让下一个查表的人得出相反的结论。
--
-- 为什么改成 0：25% 的留白是照着 16px 列表行定的，但它同样落在 TILE 模式 56px 的大磁贴上，
-- 观感是图标缩在一块白底中间盖不住背景（首页置顶区最明显）。留白该由容器自己给，不该由图片
-- 默认让出来；确实需要留白的站点由管理员按（站点 × 展示模式）单独调 —— 那正是这张表的用途。
--
-- **不动存量行。** 这张表只由人工写入，行里的值是管理员按站点调过的结论，不该被一次默认值
-- 调整抹掉。2026-08-16 查生产：全表 0 行，所有书签走的都是代码里的 fallback，因此这次改动
-- 无需任何数据回填，改代码即全量生效。
--
-- 幂等，可重复执行。

ALTER TABLE public.site_display_pref ALTER COLUMN icon_padding SET DEFAULT 0;
