-- ─────────────────────────────────────────────────────────────────────────────
-- 2026-08-17 · 移除管理员自定义图标（内边距 / 背景色 / 钉图）
--
-- ⚠️ 这条迁移必须在**部署 API 之后**应用，与本仓库绝大多数迁移方向相反
--    （同 2026-08-03_site_layering_cleanup.sql 的情形）。
--
--    运行中的旧 jar 仍在 SELECT 这张表，而那条 SELECT 位于 SiteAssetResolver.resolveBatch
--    上 —— 那是**整个桌面的读路径**。先 drop 会让每个用户的 /bookmark/query 直接抛异常。
--
--    表是空的（迁移当天生产实测：总行数 0，调过内边距 0 / 调过背景色 0 / 钉过图 0），
--    所以「部署后、迁移前」这段窗口没有任何数据风险：新代码不读它，旧代码读到的也一直是空集。
--
-- 为什么删：图标质量走**规则**（AssetRolePolicy）而不是人工逐站点微调。规则是纯函数，改一次
-- 同时作用于全部站点以及所有还没被收藏的站点；人工设置只覆盖被调过的那一个，而用户对图标质量
-- 的感受由「刚添加的那一条」决定，那一条恒为未调过。完整理由与后续计划见根目录
-- `ICON-DISPLAY-TODO.md`。
--
-- 钉图那一列还有个独立的毛病：SiteAssetWriter.replaceLayer 在资产集合有任何变化时整层删除重插，
-- 新行是新 id，于是钉的 id 匹配不上就静默回落自动排序 —— 没有报错、没有日志。将来若重做用户级
-- 图标覆盖，键必须是 content_hash（跨抓取稳定的身份），而不是 site_asset.id。
--
-- 幂等，可重复执行。
-- ─────────────────────────────────────────────────────────────────────────────

DROP INDEX IF EXISTS uk_site_display_pref_owner;
DROP TABLE IF EXISTS public.site_display_pref;
