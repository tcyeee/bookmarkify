# Bookmarkify Web 环境变量示例
# 复制本文件为 .env 后按需修改（.env 已 gitignore）。
# 生产值不来自这里：CI 在 .github/workflows/deploy-web.yml 里以 shell 环境变量注入并在构建期烤进静态产物。

# ─── 连哪个后端 ─────────────────────────────────────────────
# 就这一个开关，二选一。REST 和 WebSocket 会一起切。
NUXT_BACKEND='http://127.0.0.1:8001'
#NUXT_BACKEND='https://bookmarkify.cc'
#
# 填后端 origin，别带 /api 也别带 /ws —— 两个后缀都由 nuxt.config.ts 按环境补：
#   本地：REST = http://127.0.0.1:8001（直连 API 进程，无前缀）
#   线上：REST = https://bookmarkify.cc/api（经 nginx，deploy/nginx/bookmakify.cc.conf 的
#         `rewrite ^/api(/.*)$ $1 break` 会把 /api 剥掉再转给后端）
#   WS 两种情况都不加前缀，stores/websocket.store.ts 自己拼 `${wsBase}/ws?token=...`
#
# 和 bookmarkify-admin 的差异（不是随意的，是架构决定的）：
#   admin 有 vite dev proxy，所以它的开关 VITE_PROXY_TARGET 只改代理落点，前端代码里
#   一律用相对路径；web 没配任何 dev proxy（dev 跑在 :3000），只能把绝对地址烤进产物。
#   写法不同，但都是「一个变量、两个值、REST 和 WS 一起切」。

# ─── 站点地址 ───────────────────────────────────────────────
# 用于 SEO（og:url / og:image / canonical，见 nuxt.config.ts）以及分享链接拼接
# （components/setting/ShareManage.vue、pages/share/edit.vue）。
# 留空则回落到硬编码的 https://bookmarkify.cc —— 本地调试分享功能时建议显式设置。
NUXT_PUBLIC_SITE_URL='https://bookmarkify.cc'

# ─── 文件服务环境开关 ───────────────────────────────────────
# 决定头像 / logo / OG 图走哪里：development = 本地 /upload 代理，其他 = https://file.bookmarkify.cc
# （server/config/image.config.ts、server/routes/upload/[...path].ts）
# 与 NODE_ENV 是「或」关系，任一为 development 即视为开发环境；`pnpm dev` 下通常无需设置，
# 仅在 build/preview 时想继续吃本地 upload 目录才需要显式打开。
#NUXT_ENV='development'

# ─── 第三方登录 ─────────────────────────────────────────────
# Google 登录 OAuth Client ID（留空则不显示谷歌登录按钮）
NUXT_PUBLIC_GOOGLE_CLIENT_ID=''
# GitHub 登录 OAuth App Client ID（留空则不显示 GitHub 登录按钮）
NUXT_PUBLIC_GITHUB_CLIENT_ID=''
